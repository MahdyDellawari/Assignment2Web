function search() {
    // Get the search input value
    var searchTerm = document.getElementById("searchInput").value;

    // Perform a search (you can customize this part based on your needs)
    // For simplicity, let's just display the search term for now
    document.getElementById("searchResults").innerHTML = "Search Results: " + searchTerm;
}
